#include <bits/stdc++.h>

using namespace std;

vector<pair<vector<int>,int>> num(1e6+1);

void obtenerFactores(int n)
{
    vector<int> aux;
    vector<int> check;

    while (n>1)
    {
        for
    }


}
+
void sol(){
    int n,q; cin>>n>>q;

    vector<int> aux;
    aux.push_back(1);
    num[1].first=aux;
    num[1].second=1;
    for (int i = 2; i < 1e6; ++i)
    {

    }


}

int main()
{
    int t=1;
    cin>>t;
    while (t--) sol();
    return 0;
}
